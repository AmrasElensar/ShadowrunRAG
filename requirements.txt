# ===================
# Core Dependencies
# ===================
fastapi==0.110.0
uvicorn==0.27.1
streamlit==1.31.0
watchdog==3.0.0

# ===================
# PDF & Document Processing
# ===================
PyMuPDF==1.24.4            # fitz - superior text/layout extraction
pdf2image==1.16.3          # PDF to PIL images for OCR
pytesseract==0.3.10        # OCR engine wrapper
Pillow==11.3.0             # Image processing (required by pdf2image)
markdown==3.5.1            # Markdown parsing
unstructured[local-inference,pdf]==0.13.6
unstructured[pdf]==0.13.6
# Note: unstructured requires: libmagic, poppler, tesseract (see below)

# ===================
# AI/ML & RAG Stack
# ===================
ollama==0.2.0              # Updated API support
chromadb==0.4.24           # Persistent client, metadata filtering
langchain==0.1.18
langchain-community==0.0.33
langchain-core==0.1.45
langchain-text-splitters==0.0.1  # Explicit for MarkdownHeaderTextSplitter

# Tokenization
tiktoken==0.6.0

# Embedding backend (if you expand later)
# sentence-transformers==2.2.2  # Alternative to Ollama embeddings

# ===================
# Utilities
# ===================
python-multipart==0.0.19    # For FastAPI file uploads
aiofiles==23.2.1           # Async file I/O
pydantic==2.6.4            # Data validation (LangChain compatible)
python-dotenv==1.0.0       # Env vars
tqdm==4.66.4               # Progress bars
numpy>=1.21.0              # Required by many ML packages